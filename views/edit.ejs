<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM"
      crossorigin="anonymous"
    />
    <script
      defer
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz"
      crossorigin="anonymous"
    ></script>
    <title>Expense Tracker</title>
    <style>
      #main {
        border: 2px solid coral;
        border-radius: 5px;
        font-weight: 500;
        margin: 50px auto;
        padding: 25px;
      }
    </style>
  </head>

  <body>
    <div class="container" id="main">
      <!-- <h1>Hello</h1> -->
      <form action="/edit/<%= expense1.id %>" method="get" id="expense1">
        <div class="container">
          <label for="amount">Expense amount</label>
          <input value="<%=expense1.amount%>" type="text" name="amount" id="amount" />
          <label for="expensename">Expense Name</label>
          <input value="<%=expense1.expensename%>" type="text" name="expensename" id="desc" />
          <label  for="expensetype">Expense Type</label>
          <select name="expensetype" id="category">
            <option value="movie" <%= expense1.expensetype === 'movie' ? 'selected' : '' %>>Movie</option>
            <option value="fuel" <%= expense1.expensetype === 'fuel' ? 'selected' : '' %>>Fuel</option>
            <option value="food" <%= expense1.expensetype === 'food' ? 'selected' : '' %>>Food</option>
            <option value="drink" <%= expense1.expensetype === 'drink' ? 'selected' : '' %>>Drink</option>
          </select>
          <button type="submit" id="submit">Modify EXPENSE</button>
        </div>
      </form>
      <br /><br />
    </div>
  </body>
</html>
